---
"@zhivex-ai/core": patch
---

Fix Postgres-backed agent state, tool-journal, and memory JSON persistence, retry the catalog conflict raised by concurrent table initialization, and publish file-backed execution claims atomically.
