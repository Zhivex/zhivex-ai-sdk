---
"@zhivex-ai/core": patch
---

Avoid polynomial regex backtracking when generating subagent tool names from identifiers containing long runs of underscores, while preserving the existing naming behavior.
