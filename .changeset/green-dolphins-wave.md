---
"@zhivex-ai/core": minor
"@zhivex-ai/sdk": minor
---

Add an initial agent runtime to core and sdk with `createAgent()`, `runAgent()`, `resumeAgent()`, `streamAgent()`, serializable agent run state snapshots, a common approval flow for suspended MCP-backed agent runs, agent lifecycle/UI streaming helpers such as `toUIAgentStreamResponse()`, richer hosted-tool/agent capability metadata, and official provider agent support tiers.
