---
"@zhivex-ai/core": minor
"@zhivex-ai/sdk": minor
"@zhivex-ai/agents": minor
---

Add a stable `Agent` class facade with `run()`, `stream()`, `resume()`, and `toDefinition()`, expose the agent tool helper from `@zhivex-ai/agents`, and add agent-focused release docs, deterministic examples, Next.js streaming UI guidance, workflow/observability/workspace-agent guides, and maintainer readiness checks for full agent releases.
