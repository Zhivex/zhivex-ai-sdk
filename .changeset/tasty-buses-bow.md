---
"@zhivex-ai/ollama": minor
"@zhivex-ai/openrouter": minor
---

Add advanced provider parity features backed by current upstream documentation.

- add embeddings support to `@zhivex-ai/ollama` via the Ollama `/api/embed` endpoint
- add OpenRouter hosted web search support with `openRouterWebSearchTool()`
- allow OpenRouter server tools to be mixed with local callable tools in the shared SDK contract
